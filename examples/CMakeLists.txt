file(GLOB DIRECTORIES LIST_DIRECTORIES true "*")
file(GLOB FILES LIST_DIRECTORIES false "*")
list(REMOVE_ITEM DIRECTORIES ${FILES})

foreach(DIRECTORY ${DIRECTORIES})
  add_subdirectory(${DIRECTORY})
endforeach(DIRECTORY ${DIRECTORIES})
